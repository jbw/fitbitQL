

const fitbitTypes = `

type Activity {
    id: Int!,
    name: String
}

type Fitbit {
    Steps: Step
}

type Step {
    count: Int
}

type User {
    username:String!
}

type Query {
    activities: [Activity]
    steps: Step
}
`;

export default fitbitTypes;


